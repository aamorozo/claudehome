{
  "name": "fub-daily-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "trigger": "curl -s -X POST $WORKER_URL -H \"Authorization: Bearer $MANUAL_TRIGGER_SECRET\""
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
