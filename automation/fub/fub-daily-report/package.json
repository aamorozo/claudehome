{
  "name": "fub-daily-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "trigger": "curl -s -X POST http://localhost:8787/trigger | jq .",
    "trigger:prod": "curl -s -X POST https://fub-daily-report.<YOUR_SUBDOMAIN>.workers.dev/trigger | jq ."
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
